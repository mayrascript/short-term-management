rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // NOTE: tighten these rules once auth is added.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

